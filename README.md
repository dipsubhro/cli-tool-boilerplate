# Boiling Code

Boiling Code is a command-line tool that generates code based on your query. It provides a simple and efficient way to get code snippets without the noise.

## Installation

To get started, install the package using npm:

```bash
npm i boiling-code
```

#Example
boil "generate a function to reverse a string in JavaScript"

